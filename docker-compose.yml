version: '3.8'

services:
  bot:
    build: .
    container_name: emoji_id_bot
    restart: unless-stopped
    # Coolify will automatically inject environment variables defined in its dashboard.
    # We also include env_file for local development.
    env_file:
      - .env
